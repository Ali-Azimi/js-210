

function attendance(list) {
    let count = 0;

// es6
    // for(let i=0; i<list.length; i++) {
    //     if(list[i] === true) {
    //         count++;
    //     }
    // }
    for (const item of list) {
        if(harchi) {
            count++
        }
    }

    return count;

}